apiVersion: v1
kind: Secret

metadata:
  name: mariadb-secrets

type: Opaque

data:
  MARIADB_NEXTCLOUD_DATABASE: nextcloud_db
  MARIADB_NEXTCLOUD_USER: nextcloud
  MARIADB_NEXTCLOUD_PASSWORD: <any-base64-encoded-password>
  MARIADB_ROOT_PASSWORD: <a-different-base64-encoded-password>