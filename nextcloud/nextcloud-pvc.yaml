apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: nextcloud-pvc
  label:
    type: backend

spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 500Gi

