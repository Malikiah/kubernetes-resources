apiVersion: v1
kind: Service

metadata:
  name: nextcloud-service

spec:
  type: NodePort
  ports:
    -   targetPort: 9000
        port: 9000
        nodePort: 39000

  selector:
    app: nextcloud
    type: frontend

  component: nextcloud