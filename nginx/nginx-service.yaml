apiVersion: v1
kind: Service
metadata:
    name: nginx-service
spec:
    type: nodePort
    nodeName: fra1-elliptic-node-01
    selector:
        app: nginx
    ports:
    -   name: nginx-port
        port: 80
        targetport: 80
        nodeport: 30080
