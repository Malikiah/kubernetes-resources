apiVersion: v1
kind: Ingress
metadata:
    name: nginx-service
spec:
    type: NodePort
    selector:
        app: nginx
    ports:
    -   name: nginx-port
        targetPort: 80
        port: 80
        nodePort: 30080
