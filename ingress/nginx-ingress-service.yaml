apiVersion: v1
kind: Service

metadata:
  name: nginx-ingress-controller
  labels:
    app: nginx-ingress-controller

spec:
  type: 'NodePort'

  selector:
    app: nginx-ingress-controller

  ports:
  - name: http
    port: 80
    targetPort: http
    NodePort: 80
  - name: https
    port: 443
    targetPort: https
    NodePort: 443