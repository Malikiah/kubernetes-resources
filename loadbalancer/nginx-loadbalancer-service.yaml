# Service - LoadBalancer
apiVersion: v1
kind: Service
metadata:
    name: nginx-loadbalancer-service
spec:
    type: LoadBalancer
    selector:
        app: nginx-loadbalancer
    ports:
    -   name: nginx-loadbalancer-port
        targetPort: 30080
        port: 30080
        nodePort: 80